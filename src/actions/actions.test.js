import * as adminActions from './adminActions';
import * as types from './adminActionTypes';

test('admin products return actions', () => {
  //let products = adminActions.getAdminProducts();
  //return {type: types.LOAD_ADMIN_PRODUCTS_SUCESS, admin_products: products}
  //expect(products.type).toEqual(types.LOAD_ADMIN_PRODUCTS_SUCESS);
  //expect(products.admin_products.length > 1).toBeTruthy();

});
